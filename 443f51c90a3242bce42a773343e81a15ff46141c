{"category": "Changed", "changelog_entry": "Clarified meeting minutes as work-log concept documents, requiring OKF-style YAML frontmatter and strict filename conventions, to ensure machine-friendly and consistent output for downstream workflows.", "summary": "Meeting minutes are now defined as work-log concept documents, requiring OKF-style YAML frontmatter and strict filename conventions. The protocol and template have been updated to specify required and recommended frontmatter fields, default tags, and file generation steps. This change ensures output is machine-friendly and consistent for downstream workflows, with the template now including a frontmatter block and improved formatting.", "version": 1}
