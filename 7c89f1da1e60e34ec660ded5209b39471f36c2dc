{"category": "Added", "changelog_entry": "Enabled exclusive management of repair knowledge via a structured CLI and schema, replacing ad hoc repair notes and providing actionable guidance for new failures through outcome tracking.", "summary": "A structured repair knowledge base has been introduced to capture, abstract, and reuse completed fixes across repositories. The knowledge base is managed exclusively via a new CLI and schema, replacing prior ad hoc or unstructured repair notes. The CLI, `scripts/kb.py`, provides templating, validation, indexing, querying, and outcome recording for repair nodes, which are organized by abstraction level and repair stage with enforced schema and provenance. This enables retrieval of actionable guidance for new failures, prevents duplication, and supports continuous improvement through outcome tracking.", "version": 1}
